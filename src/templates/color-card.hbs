{{!-- <div class="color-card">
    <div class="color-swatch" data-hex="{{hex}}" data-rgb="{{rgb}}" style="background-color: {{hex}}"></div>
    <div class="color-meta">
        <p>HEX: {{hex}}</p>
        <p>RGB: {{rgb}}</p>
    </div>
</div> --}}


{{#each this}} 
<div class="color-card">
    <div class="color-swatch" data-hex="{{hex}}" data-rgb="{{rgb}}" style="background-color: {{hex}}"></div>
    <div class="color-meta">
        <p>HEX: {{hex}}</p>
        <p>RGB: {{rgb}}</p>
    </div>
</div>
{{/each}}

{{! 
-> hbs form file possesses its own cycles and other types of functions
like:
* if
* each


More detailed https://handlebarsjs.com/guide/builtin-helpers.html#if
--}}